function itemSelected(index, arr) {
    console.log(index, arr)
    document.getElementById('selectedItem').innerHTML = arr[index];
}


/**
 * Need to add categories to items and be able to limit items to those
 * As well as brands
 * 
 */